import { SideBar } from "../shared/components/SideBar/SideBar";

export const Test = () => {
  return (
    <div className="flex min-h-screen">
      <SideBar />
      <div>OLA</div>
    </div>
  );
};
